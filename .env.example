# Public origins. OMNIFIN_BASE_URL is an origin only: no credentials, path,
# query, or fragment. Use the externally visible HTTPS origin in production.
OMNIFIN_BASE_URL=http://localhost:3000
OMNIFIN_GATEWAY_URL=http://127.0.0.1:4000
# Keep zero for direct source access. Set this to the exact number of maintained
# reverse-proxy hops only when direct access to the web process is blocked.
OMNIFIN_WEB_TRUST_PROXY_HOPS=0

# Runtime
NEXT_TELEMETRY_DISABLED=1
STORYBOOK_DISABLE_TELEMETRY=1
TURBO_TELEMETRY_DISABLED=1
OMNIFIN_HOST=127.0.0.1
OMNIFIN_INSECURE_LOOPBACK_PREVIEW=true
OMNIFIN_PORT=4000
OMNIFIN_DATABASE_URL=./data/omnifin.db
OMNIFIN_LOG_LEVEL=info
OMNIFIN_SECURE_COOKIES=false
OMNIFIN_TRUST_PROXY_HOPS=0

# Use ten-foot for television-distance typography, focus, and control sizing.
OMNIFIN_DISPLAY_PROFILE=standard

# Generate exactly 32 random bytes and encode them as canonical base64. Direct
# source development may use the value variable. Compose instead reads the file
# path (default `./secrets/omnifin_encryption_key`) and mounts it read-only into
# the gateway. Keep either form out of source control and separate from backups.
OMNIFIN_ENCRYPTION_KEY=
OMNIFIN_ENCRYPTION_KEY_FILE=

# Supplying a Jellyfin URL enables the single Jellyfin connector used for sign-in,
# Quick Connect, account pairing, and permission-checked media workflows. Configure
# additional services after establishing the first Omnifin administrator.
# Use HTTPS. A private-network HTTP endpoint is rejected unless the separate,
# service-specific acknowledgement below is deliberately set to true.
OMNIFIN_JELLYFIN_URL=
OMNIFIN_JELLYFIN_INSECURE_HTTP_APPROVED=false

# Hidden break-glass recovery. Generate 32 to 128 random bytes and encode them as
# canonical base64. Direct source development may use the value variable. Compose
# reads the file path (default `./secrets/omnifin_recovery_secret`); keep it separate
# from the database and encryption key, and do not expose `/recovery` in ordinary login navigation. A
# fresh installation uses this route to make one recovery-bound claim through either
# a configured OIDC provider or current Jellyfin administrator proof.
OMNIFIN_RECOVERY_SECRET=
OMNIFIN_RECOVERY_SECRET_FILE=

# Synthetic interface data for local visual review only. This does not enable
# authentication, connectors, media operations, or a supported deployment.
OMNIFIN_DEMO_MODE=false
